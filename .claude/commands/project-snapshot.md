---
description: Create a concise AgenticOS project snapshot
---

Produce a current project snapshot for another agent or future session.

Include:

- Current git status.
- Changed files grouped by feature area.
- Runtime commands available in `package.json`.
- Open risks in high-blast-radius modules.
- Relevant `.agenticos-project/` context files.
- Recommended next verification command.

Keep the snapshot factual. Do not claim tests passed unless they were run in this session.

Arguments: `$ARGUMENTS`
